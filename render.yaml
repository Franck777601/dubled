services:
  - type: web
    name: ispa-forms
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: WHATSAPP_PHONE
        value: 237677688000
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
